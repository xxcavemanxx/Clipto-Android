<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <clipto.presentation.main.list.MainListViewLayout
        android:id="@+id/mainView"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?attr/colorPrimaryInverse"
            android:clipToPadding="false"
            android:paddingBottom="96dp"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimaryInverse"
            app:elevation="0dp">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_margin="16dp"
                android:background="@drawable/bg_main_toolbar"
                android:elevation="0dp"
                app:collapseIcon="@drawable/action_arrow_back"
                app:contentInsetStart="0dp"
                app:contentInsetStartWithNavigation="0dp"
                app:elevation="0dp"
                app:layout_scrollFlags="scroll|enterAlways"
                app:titleTextAppearance="?android:titleTextAppearance"
                app:titleTextColor="?android:textColorPrimary">

                <com.google.android.material.textfield.TextInputLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:counterEnabled="false"
                    app:endIconMode="clear_text"
                    app:expandedHintEnabled="false"
                    app:hintEnabled="false">

                    <EditTextExt
                        android:id="@+id/searchView"
                        style="@style/TextInputEditText"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="@null"
                        android:fontFamily="sans-serif"
                        android:gravity="center_vertical"
                        android:hint="@string/main.search"
                        android:imeActionLabel="@string/main.search"
                        android:imeOptions="actionSearch"
                        android:inputType="text"
                        android:maxLines="1"
                        android:minLines="1"
                        android:singleLine="true"
                        android:textAlignment="gravity"
                        android:textSize="16sp" />

                </com.google.android.material.textfield.TextInputLayout>

            </androidx.appcompat.widget.Toolbar>

        </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.bottomappbar.BottomAppBar
            android:id="@+id/bottomBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:backgroundTint="@android:color/transparent"
            android:gravity="center_vertical"
            android:textAlignment="gravity"
            android:theme="@style/MyBottomBar"
            app:contentInsetStartWithNavigation="0dp"
            app:hideOnScroll="true"
            app:navigationContentDescription="@string/menu.cancel"
            app:titleTextColor="?attr/actionIconColorNormal"
            tools:ignore="BottomAppBar">

            <FrameLayout
                android:id="@+id/selectionAllContainer"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/selectionAllAction"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_gravity="center_vertical"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/desktop.shortcuts.contextModeSelectAll"
                    android:padding="12dp"
                    app:srcCompat="@drawable/ic_select_all" />

                <TextViewExt
                    android:id="@+id/selectionCounter"
                    android:layout_width="wrap_content"
                    android:layout_height="18dp"
                    android:layout_gravity="top|end"
                    android:layout_marginStart="24dp"
                    android:layout_marginTop="8dp"
                    android:background="@drawable/bg_counter"
                    android:fontFamily="sans-serif"
                    android:gravity="center_vertical"
                    android:maxLines="1"
                    android:minLines="1"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:singleLine="true"
                    android:textAlignment="gravity"
                    android:textColor="#fff"
                    android:textSize="12sp" />

            </FrameLayout>
        </com.google.android.material.bottomappbar.BottomAppBar>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/actionButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/menu.new"
            android:src="@drawable/ic_add_black"
            app:backgroundTint="?colorAccent"
            app:layout_anchor="@id/bottomBar"
            app:layout_anchorGravity="center_horizontal"
            app:tint="?textColorOnSurface" />

    </clipto.presentation.main.list.MainListViewLayout>

    <FrameLayout
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="?attr/colorPrimaryInverse"
        android:fitsSystemWindows="true">

        <fragment
            android:id="@+id/mainNav"
            android:name="clipto.presentation.main.nav.MainNavFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </FrameLayout>

</androidx.drawerlayout.widget.DrawerLayout>