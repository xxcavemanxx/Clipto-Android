<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="40dp"
    android:layout_marginStart="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_abbreviation"
    android:orientation="horizontal">

    <EditTextExt
        android:id="@+id/etClipAbbreviation"
        style="@style/TextInputEditText"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="6dp"
        android:background="@null"
        android:fontFamily="sans-serif-condensed"
        android:gravity="center_vertical"
        android:hint="@string/common.label.abbreviation"
        android:imeOptions="actionDone"
        android:maxLines="1"
        android:minWidth="72dp"
        android:minLines="1"
        android:overScrollMode="never"
        android:singleLine="true"
        android:textAlignment="gravity"
        android:textColor="?android:textColorPrimary"
        android:textColorHint="?android:textColorSecondary"
        android:textIsSelectable="true"
        android:textSize="14sp"
        app:layout_constraintEnd_toStartOf="@id/ivHintAbbreviation"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_goneMarginEnd="12dp" />

    <ImageView
        android:id="@+id/ivHintAbbreviation"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="?selectableItemBackgroundBorderless"
        android:padding="6dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_floating_hint" />

</androidx.constraintlayout.widget.ConstraintLayout>